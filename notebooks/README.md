# Notebooks

Exploratory notebooks and their rendered outputs live here.

- `exploratory/` contains source `.Rmd` and ad hoc `.R` files.
- `rendered/` contains rendered HTML files from exploratory notebooks.

Most notebooks assume the working directory is the repository root. If opening a
notebook directly from this folder, set the working directory back to the project
root before running chunks.
